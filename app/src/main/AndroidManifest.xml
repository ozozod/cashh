<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:label="VayVene"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <activity
            android:usesCleartextTraffic="true"
            android:networkSecurityConfig="@xml/network_security_config"
            android:name=".ui.login.NfcLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.admin.AdminMenuActivity" android:exported="false"/>
        <activity android:name=".ui.admin.RegisterCardActivity" android:exported="false"/>
        <activity android:name=".ui.admin.RegisterResultActivity" android:exported="false"/>
        <activity android:name=".ui.main.SellerMenuActivity" />
        <activity android:name=".ui.main.SellerSummaryActivity" />
        <activity android:name=".ui.main.CancelSaleActivity" />
        <activity android:name=".ui.main.PosActivity" />
        <activity android:name=".ui.main.BalanceActivity" />

    </application>
</manifest>
